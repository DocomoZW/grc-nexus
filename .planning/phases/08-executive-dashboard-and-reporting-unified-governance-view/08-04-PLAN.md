---
phase: 08-executive-dashboard-and-reporting-unified-governance-view
plan: 04
type: execute
wave: 4
depends_on:
  - 08-03
files_modified:
  - app/(protected)/admin/audit-log/page.tsx
  - app/(protected)/admin/audit-log/FilterBar.tsx
  - app/api/audit/export/route.ts
  - .planning/phases/08-executive-dashboard-and-reporting-unified-governance-view/08-VERIFICATION.md
autonomous: false
requirements:
  - RPT-04
must_haves:
  truths:
    - "Audit trail supports filtering by module and department in addition to existing filters."
    - "CSV export respects all active filter dimensions."
    - "Verification artifact captures user-facing proof for phase-8 outputs."
  artifacts:
    - path: "app/(protected)/admin/audit-log/FilterBar.tsx"
      provides: "Extended filter UI"
      contains: "module and department selectors"
    - path: "app/api/audit/export/route.ts"
      provides: "Filter-aware CSV export"
      exports: ["GET"]
    - path: ".planning/phases/08-executive-dashboard-and-reporting-unified-governance-view/08-VERIFICATION.md"
      provides: "Execution verification evidence"
---

<objective>
Finalize phase 8 by extending audit trail filtering/export and capturing verification evidence.

Purpose: Deliver RPT-04 and close phase with demonstrable outcomes.
Output: Updated audit-log filter/export behavior and verification document.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@app/(protected)/admin/audit-log/page.tsx
@app/(protected)/admin/audit-log/FilterBar.tsx
@app/api/audit/export/route.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend audit filter model and CSV export scope</name>
  <files>app/(protected)/admin/audit-log/FilterBar.tsx, app/(protected)/admin/audit-log/page.tsx, app/api/audit/export/route.ts</files>
  <action>Add module + department filters across page query parser, filter controls, data query, and export endpoint so CSV output matches visible table scope.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Audit trail filtering/export satisfies RPT-04.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verify end-to-end executive reporting behavior</name>
  <details>
    - Visit `/dashboard`, apply filters, refresh page, confirm URL persistence.
    - Download governance PDF and verify rendered sections.
    - Visit `/admin/audit-log`, apply module/department filters, export CSV, verify rows align.
    - Record outcomes/screenshots in 08-VERIFICATION.md.
  </details>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit log filter params -> SQL query | Malformed filters can create incorrect or broad result sets |
| Audit CSV export -> downloaded artifact | Export may include data beyond filtered scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | T | audit filter parser | mitigate | Validate module/department values and normalize defaults |
| T-08-08 | I | audit CSV export | mitigate | Ensure export uses identical where-clause logic as UI table query |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Human verification checklist completed and documented in `08-VERIFICATION.md`.
</verification>

<success_criteria>
- RPT-04 complete and phase verification artifact written.
</success_criteria>

<output>
After completion, create `.planning/phases/08-executive-dashboard-and-reporting-unified-governance-view/08-04-SUMMARY.md` and update `08-VERIFICATION.md`.
</output>
