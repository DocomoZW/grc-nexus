---
phase: 06-internal-audit-findings-and-remediation
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - app/(protected)/audit/page.tsx
  - app/(protected)/audit/findings/page.tsx
  - app/(protected)/audit/findings/AuditFindingsTable.tsx
  - app/(protected)/audit/findings/new/page.tsx
  - app/(protected)/audit/findings/new/AuditFindingForm.tsx
  - components/audit/AuditStatCard.tsx
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-04
must_haves:
  truths:
    - "Audit dashboard shows open finding counts grouped by severity"
    - "Audit users can open findings list and filter by severity/status/owner"
    - "Audit officers can create findings with required linkage and remediation assignment"
  artifacts:
    - path: "app/(protected)/audit/page.tsx"
      provides: "Audit overview dashboard route"
      contains: "open findings by severity"
    - path: "app/(protected)/audit/findings/AuditFindingsTable.tsx"
      provides: "Client table with filters for audit findings"
      exports: ["AuditFindingsTable"]
    - path: "app/(protected)/audit/findings/new/AuditFindingForm.tsx"
      provides: "Validated form for finding creation"
      exports: ["AuditFindingForm"]
  key_links:
    - from: "app/(protected)/audit/page.tsx"
      to: "lib/audit/queries.ts"
      via: "Dashboard server queries"
      pattern: "getAuditDashboardStats"
    - from: "app/(protected)/audit/findings/new/AuditFindingForm.tsx"
      to: "lib/audit/actions.ts"
      via: "Form submission to create action"
      pattern: "createAuditFinding"
---

<objective>
Deliver audit dashboard and finding registration interfaces for day-to-day audit officer workflows.

Purpose: Make findings visible and creatable in the protected app shell.
Output: Audit routes for overview, listing, and new finding creation with filtering and summary metrics.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-internal-audit-findings-and-remediation/06-RESEARCH.md
@app/(protected)/compliance/page.tsx
@app/(protected)/compliance/obligations/page.tsx
@app/(protected)/compliance/obligations/ObligationsTable.tsx
@components/compliance/ComplianceStatCard.tsx
@components/compliance/ObligationFilterBar.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build audit overview page and stat card component</name>
  <files>app/(protected)/audit/page.tsx, components/audit/AuditStatCard.tsx</files>
  <action>Create `/audit` overview route as a protected server component that loads open findings by severity and overdue remediation totals via audit queries. Implement `AuditStatCard` with existing card token styles so visual language matches compliance and risk dashboards.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Overview route renders live severity/overdue metrics and respects protected layout conventions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build findings list route with filterable table</name>
  <files>app/(protected)/audit/findings/page.tsx, app/(protected)/audit/findings/AuditFindingsTable.tsx</files>
  <action>Create findings listing route backed by server-side data load and a client-side table for sorting/filtering by severity, status, and owner. Reuse established table styling, empty-state handling, and status badge patterns from compliance and risk modules.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Audit users can browse and filter findings without page reload and navigate to finding details.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build new finding form with linked-entity and remediation owner fields</name>
  <files>app/(protected)/audit/findings/new/page.tsx, app/(protected)/audit/findings/new/AuditFindingForm.tsx</files>
  <action>Implement RHF + Zod form for finding creation including severity, root cause, review date, narrative, linked entity type/id, due date, and remediation owner. Submit via audit server action and redirect to findings list or detail on success with toast feedback on error.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Audit officers can create complete findings that satisfy AUDIT-01 and AUDIT-02 data requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form inputs -> server actions | User can submit malformed or unauthorized finding payloads |
| Protected routes -> institution data | Misconfigured query filters could expose cross-institution data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | T | new finding form payload | mitigate | Validate via Zod schema in action; reject invalid linked entity and severity values |
| T-06-10 | I | findings list and dashboard | mitigate | Use RLS-scoped server client and avoid admin bypass clients in read routes |
| T-06-11 | E | create finding route access | mitigate | Gate pages/actions to `admin` and `audit-officer` roles; redirect unauthorized roles |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `/audit`, `/audit/findings`, and `/audit/findings/new` load in authenticated sessions.
</verification>

<success_criteria>
- Audit dashboard and finding intake UI satisfy AUDIT-04 visibility and AUDIT-01/02 capture requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/06-internal-audit-findings-and-remediation/06-03-SUMMARY.md`
</output>
