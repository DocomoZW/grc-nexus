---
phase: 08-executive-dashboard-and-reporting-unified-governance-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/reporting/filters.ts
  - lib/reporting/queries.ts
  - lib/reporting/types.ts
  - tests/reporting/reporting-queries.test.ts
autonomous: true
requirements:
  - RPT-01
  - RPT-02
must_haves:
  truths:
    - "A shared dashboard filter contract exists for date range, department, and module dimensions."
    - "Reporting query helpers aggregate KPI, risk, compliance, and overdue action metrics from existing module tables."
    - "Query layer returns typed payloads ready for consolidated dashboard widgets."
  artifacts:
    - path: "lib/reporting/filters.ts"
      provides: "Validated filter parser and normalized defaults"
      exports: ["reportingFilterSchema", "normalizeReportingFilters"]
    - path: "lib/reporting/queries.ts"
      provides: "Consolidated executive metric queries"
      exports: ["getExecutiveDashboardData"]
    - path: "lib/reporting/types.ts"
      provides: "Typed contracts for dashboard payloads"
      exports: ["ExecutiveDashboardData", "ReportingFilters"]
  key_links:
    - from: "lib/reporting/queries.ts"
      to: "lib/reporting/filters.ts"
      via: "normalized filter contract for all metrics"
      pattern: "normalizeReportingFilters"
---

<objective>
Create the phase 8 data foundation for executive reporting: shared filters and consolidated query helpers.

Purpose: Dashboard UI and export routes depend on this contract and aggregated payload shape.
Output: New reporting library files and tests.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-executive-dashboard-and-reporting-unified-governance-view/08-RESEARCH.md
@app/(protected)/compliance/page.tsx
@app/(protected)/risk/heatmap/page.tsx
@lib/compliance/queries.ts
@lib/risk/queries.ts
@lib/strategic/queries.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define reporting filters/types and failing tests</name>
  <files>lib/reporting/filters.ts, lib/reporting/types.ts, tests/reporting/reporting-queries.test.ts</files>
  <behavior>
    - Test 1: invalid date range is rejected.
    - Test 2: unknown module values are rejected.
    - Test 3: normalized filters include defaults when params are omitted.
  </behavior>
  <action>Create reporting types and zod filter schema plus normalization helper; write tests first.</action>
  <verify>
    <automated>npx vitest run tests/reporting/reporting-queries.test.ts</automated>
  </verify>
  <done>Filter contract and tests are established.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement consolidated query helper</name>
  <files>lib/reporting/queries.ts</files>
  <action>Implement `getExecutiveDashboardData` that composes KPI summary, risk heatmap points, compliance posture, and top overdue governance actions across modules using scoped Supabase client queries.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Dashboard data can be fetched in one typed call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query params -> reporting filters | Untrusted query strings can cause malformed query composition |
| Dashboard query layer -> multi-module data | Incorrect joins/scopes can leak cross-module or cross-department data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T | filter parser | mitigate | Strict zod validation and normalized defaults |
| T-08-02 | I | consolidated queries | mitigate | Preserve institution-scoped Supabase queries and role-gated access at page layer |
</threat_model>

<verification>
- `npx vitest run tests/reporting/reporting-queries.test.ts` passes.
- `npx tsc --noEmit` passes.
</verification>

<success_criteria>
- Phase 8 reporting query contract exists and is reusable by dashboard and export endpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/08-executive-dashboard-and-reporting-unified-governance-view/08-01-SUMMARY.md`
</output>
