---
phase: 06-internal-audit-findings-and-remediation
plan: 05
type: execute
wave: 4
depends_on:
  - 06-03
  - 06-04
files_modified:
  - app/(protected)/layout.tsx
autonomous: false
requirements:
  - AUDIT-04
  - AUDIT-05
must_haves:
  truths:
    - "Authorized roles can discover and open the audit module from protected navigation"
    - "Core phase 6 flows are verified end-to-end before phase close"
  artifacts:
    - path: "app/(protected)/layout.tsx"
      provides: "Audit nav entry integrated into protected shell"
      contains: "href='/audit'"
  key_links:
    - from: "app/(protected)/layout.tsx"
      to: "app/(protected)/audit/page.tsx"
      via: "Primary navigation entry"
      pattern: "href=\"/audit\""
---

<objective>
Wire audit module navigation and run structured end-to-end verification for phase closure.

Purpose: Ensure feature discoverability and validate that all phase requirements are truly operational.
Output: Protected sidebar nav update plus verification checkpoint outcomes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@app/(protected)/layout.tsx
@app/(protected)/audit/page.tsx
@app/(protected)/audit/findings/page.tsx
@app/(protected)/audit/findings/new/page.tsx
@app/api/audit/escalate/route.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add audit module nav entry to protected sidebar</name>
  <files>app/(protected)/layout.tsx</files>
  <action>Add an `Audit` sidebar link in protected layout, following existing nav class and role-gating patterns used for strategic/risk/compliance items. Keep unauthorized routes protected at route/action layer; sidebar visibility is a secondary affordance only.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Audit entry is visible to intended roles and points to `/audit`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validate phase 6 workflows in browser</name>
  <what-built>Audit dashboard, finding creation, status progression, evidence upload, and escalation route were implemented in plans 06-01 through 06-04.</what-built>
  <how-to-verify>
    1. Open `/audit` and confirm open findings by severity plus overdue remediation count are visible.
    2. Open `/audit/findings/new` and create a finding with linked entity, severity, root cause, review date, and remediation owner.
    3. Open the created finding detail and transition status from `open` to `in_progress`, then `closed`.
    4. Upload at least one remediation evidence file and confirm it appears in the finding evidence section.
    5. Call `POST /api/audit/escalate` once without secret (expect 401), then with valid `x-cron-secret` (expect success JSON).
    6. Confirm finding list and dashboard metrics update after status changes.
  </how-to-verify>
  <verify>
    <automated>npm run dev</automated>
  </verify>
  <resume-signal>Type "approved" or describe issues to resolve.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sidebar navigation -> protected routes | UI visibility alone is not an authorization mechanism |
| Human verification process -> release decision | False positives can ship broken access paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | E | nav access assumptions | mitigate | Keep route/action authorization checks independent from sidebar visibility |
| T-06-16 | R | phase-close confidence | mitigate | Require explicit browser workflow verification before approval |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Human verification checkpoint completed and explicitly approved.
</verification>

<success_criteria>
- Audit module is navigable and all phase workflows are verified for operational readiness.
</success_criteria>

<output>
After completion, create `.planning/phases/06-internal-audit-findings-and-remediation/06-05-SUMMARY.md`
</output>
