---
phase: 06-internal-audit-findings-and-remediation
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - lib/audit/actions.ts
  - lib/audit/queries.ts
  - lib/audit/escalation.ts
  - app/api/audit/escalate/route.ts
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-05
must_haves:
  truths:
    - "Audit officers can create findings linked to risk, control, or compliance obligations"
    - "Remediation owners can move finding status through allowed transitions and attach closure evidence"
    - "Overdue remediation escalations notify owner and audit officers"
  artifacts:
    - path: "lib/audit/actions.ts"
      provides: "Server Actions for finding create/update/status/evidence operations"
      exports: ["createAuditFinding", "updateAuditFindingStatus", "uploadAuditFindingEvidence"]
    - path: "lib/audit/queries.ts"
      provides: "List/detail/dashboard/escalation queries for audit findings"
      exports: ["listAuditFindings", "getAuditFindingById", "getAuditDashboardStats", "getAuditFindingsForEscalation"]
    - path: "lib/audit/escalation.ts"
      provides: "Email escalation orchestration for overdue remediations"
      exports: ["sendAuditEscalationEmails"]
    - path: "app/api/audit/escalate/route.ts"
      provides: "CRON invocable escalation endpoint with shared-secret guard"
      exports: ["POST"]
  key_links:
    - from: "app/api/audit/escalate/route.ts"
      to: "lib/audit/escalation.ts"
      via: "Route delegates escalation dispatch"
      pattern: "sendAuditEscalationEmails"
    - from: "lib/audit/actions.ts"
      to: "lib/schemas/audit-findings.ts"
      via: "Action payload validation"
      pattern: "safeParse"
---

<objective>
Implement the audit service layer: secure server mutations, read queries, and escalation automation endpoint.

Purpose: Provide all backend capability required by the UI plans and satisfy status/evidence/escalation requirements.
Output: Actions, query helpers, escalation service, and cron endpoint for phase 6.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-internal-audit-findings-and-remediation/06-RESEARCH.md
@lib/compliance/actions.ts
@lib/compliance/queries.ts
@lib/compliance/escalation.ts
@app/api/compliance/escalate/route.ts
@types/auth.ts
@lib/files/checksum.ts

<interfaces>
From `types/auth.ts`:
```typescript
export type AppRole =
  | 'admin'
  | 'board-member'
  | 'ceo'
  | 'risk-officer'
  | 'audit-officer'
  | 'compliance-officer'
  | 'dept-head'
```

From `lib/compliance/actions.ts` (pattern to mirror):
```typescript
async function getWriteContext(allowedRoles: AppRole[] = WRITE_ROLES)
export async function createObligation(values: ObligationInput): Promise<ActionResult>
export async function uploadEvidence(formData: FormData): Promise<ActionResult>
```

From `app/api/compliance/escalate/route.ts` (security gate pattern):
```typescript
const secret = request.headers.get('x-cron-secret')
if (!secret || secret !== process.env.CRON_SECRET) {
  return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement validated audit finding mutations</name>
  <files>lib/audit/actions.ts</files>
  <behavior>
    - Test 1: unauthorized or wrong-role users receive permission errors.
    - Test 2: valid finding creation persists with institution and actor context.
    - Test 3: remediation status updates enforce legal transitions and revalidate audit routes.
  </behavior>
  <action>Create `lib/audit/actions.ts` with role-aware context helper and actions for create/update/status/evidence upload. Mirror compliance action style (`safeParse`, typed result union, `revalidatePath`) but enforce audit-specific roles (`admin`, `audit-officer` for creation; owner pathway for status updates as defined in schema). Use server-side checksum verification for evidence uploads.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Mutation entry points exist with schema validation, role gates, institution scoping, and path revalidation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement audit queries and dashboard/escalation selectors</name>
  <files>lib/audit/queries.ts</files>
  <action>Build list/detail query helpers with owner/profile joins and ordered timelines. Add dashboard stat queries for open-by-severity and overdue remediation count. Add escalation candidate query with threshold filtering equivalent to governance patterns already used in risk/compliance.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Audit UI can fetch all required list/detail/dashboard datasets using reusable query helpers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implement escalation service and protected cron route</name>
  <files>lib/audit/escalation.ts, app/api/audit/escalate/route.ts</files>
  <action>Create escalation dispatcher using Resend and institution recipient lookup for remediation owner and audit-officer users. Add `POST /api/audit/escalate` with strict `x-cron-secret` validation before any DB or email operation. Return structured counts for found/sent/skipped outcomes.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Escalation workflow is callable by scheduler and safely blocked for unauthorized callers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scheduler/HTTP caller -> escalation route | External caller can attempt to trigger notifications |
| Authenticated user -> mutation actions | User input could attempt cross-tenant or role-bypass writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | S | `POST /api/audit/escalate` | mitigate | Require `x-cron-secret` match before any DB query |
| T-06-06 | E | `lib/audit/actions.ts` | mitigate | Enforce role allowlists and institution IDs from JWT app metadata |
| T-06-07 | T | evidence upload path | mitigate | Server-side SHA-256 recomputation + immutable path with `upsert: false` |
| T-06-08 | I | escalation query leakage | mitigate | Query through RLS-scoped client and role-filtered recipient resolution |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Manual `POST /api/audit/escalate` without secret returns 401.
</verification>

<success_criteria>
- Backend supports all finding lifecycle operations and escalation automation required for AUDIT-01/02/03/05.
</success_criteria>

<output>
After completion, create `.planning/phases/06-internal-audit-findings-and-remediation/06-02-SUMMARY.md`
</output>
