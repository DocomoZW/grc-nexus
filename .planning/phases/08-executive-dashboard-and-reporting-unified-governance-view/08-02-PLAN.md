---
phase: 08-executive-dashboard-and-reporting-unified-governance-view
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - app/(protected)/dashboard/page.tsx
  - app/(protected)/dashboard/ExecutiveFilterBar.tsx
  - app/(protected)/dashboard/KpiSummaryCard.tsx
  - app/(protected)/dashboard/OverdueActionsTable.tsx
autonomous: true
requirements:
  - RPT-01
  - RPT-02
must_haves:
  truths:
    - "Dashboard shows consolidated KPI, risk, compliance, and top overdue actions in one page."
    - "Users can filter by time period, department, and module without full page reload."
    - "Filter state persists in URL query params and survives refresh/share."
  artifacts:
    - path: "app/(protected)/dashboard/page.tsx"
      provides: "Unified executive dashboard route"
      contains: "force-dynamic"
    - path: "app/(protected)/dashboard/ExecutiveFilterBar.tsx"
      provides: "Client filter controls synced to URL"
      exports: ["ExecutiveFilterBar"]
  key_links:
    - from: "app/(protected)/dashboard/page.tsx"
      to: "lib/reporting/queries.ts"
      via: "server fetch of consolidated metrics"
      pattern: "getExecutiveDashboardData"
---

<objective>
Replace the dashboard placeholder with the phase 8 consolidated executive dashboard and URL-persisted filters.

Purpose: Deliver RPT-01 and RPT-02 observable behavior.
Output: Updated dashboard route and filter/widget components.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-executive-dashboard-and-reporting-unified-governance-view/08-RESEARCH.md
@lib/reporting/queries.ts
@app/(protected)/compliance/page.tsx
@app/(protected)/risk/heatmap/page.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build executive filter bar with URL persistence</name>
  <files>app/(protected)/dashboard/ExecutiveFilterBar.tsx</files>
  <action>Create client filter UI for date range, department, and module values; write search params via router push/replace; preserve existing values and reset page context safely.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Filter component updates URL query params without page reload.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace dashboard placeholder with consolidated widgets</name>
  <files>app/(protected)/dashboard/page.tsx, app/(protected)/dashboard/KpiSummaryCard.tsx, app/(protected)/dashboard/OverdueActionsTable.tsx</files>
  <action>Use `getExecutiveDashboardData` to render KPI summary, risk heatmap snapshot, compliance posture card, and top 10 overdue governance actions table. Keep existing role gating and dynamic rendering.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Executive dashboard presents unified governance posture and filterable content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client filter controls -> server route params | User-controlled params can be malformed/tampered |
| Consolidated page rendering -> confidential governance metrics | Cached data could leak if not dynamic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | T | dashboard filters | mitigate | Validate and normalize params before querying |
| T-08-04 | I | dashboard SSR caching | mitigate | Keep `dynamic = 'force-dynamic'` and scoped auth checks |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `/dashboard` shows consolidated widgets with active filters reflected in URL.
</verification>

<success_criteria>
- RPT-01 and RPT-02 are observable and functional on the dashboard route.
</success_criteria>

<output>
After completion, create `.planning/phases/08-executive-dashboard-and-reporting-unified-governance-view/08-02-SUMMARY.md`
</output>
