---
phase: 06-internal-audit-findings-and-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260522000023_audit_schema.sql
  - supabase/migrations/20260522000024_audit_rls.sql
  - supabase/migrations/20260522000025_audit_triggers.sql
  - types/audit.ts
  - lib/schemas/audit-findings.ts
  - lib/audit/audit-utils.ts
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
must_haves:
  truths:
    - "Audit officers can persist findings with severity, root cause, owner, review date, and linked source entity"
    - "Institution boundaries are enforced at the DB layer for all audit finding and evidence rows"
    - "All audit finding writes generate immutable audit trail entries"
  artifacts:
    - path: "supabase/migrations/20260522000023_audit_schema.sql"
      provides: "Audit finding and evidence schema plus enums and indexes"
      contains: "create table public.audit_findings"
    - path: "supabase/migrations/20260522000024_audit_rls.sql"
      provides: "RLS and role guards for audit tables"
      contains: "enable row level security"
    - path: "supabase/migrations/20260522000025_audit_triggers.sql"
      provides: "Audit trigger attachments for new audit tables"
      contains: "audit.attach_audit_trigger"
    - path: "types/audit.ts"
      provides: "Audit module domain types and label maps"
      exports: ["AuditFindingSeverity", "AuditFindingStatus", "AuditLinkedEntityType"]
    - path: "lib/schemas/audit-findings.ts"
      provides: "Zod v3 schemas for finding create/update/status/evidence payloads"
      exports: ["auditFindingSchema", "auditFindingStatusSchema"]
  key_links:
    - from: "lib/schemas/audit-findings.ts"
      to: "types/audit.ts"
      via: "Schema input/output type inference"
      pattern: "z.infer"
    - from: "supabase/migrations/20260522000024_audit_rls.sql"
      to: "supabase/migrations/20260522000023_audit_schema.sql"
      via: "RLS policies reference newly created tables"
      pattern: "audit_findings"
---

<objective>
Create the internal-audit foundation contracts: schema, RLS, triggers, domain types, validation schemas, and utility helpers.

Purpose: All service and UI plans depend on these stable contracts and database guarantees.
Output: Three SQL migrations plus audit domain TypeScript contracts and helper modules.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-internal-audit-findings-and-remediation/06-RESEARCH.md
@supabase/migrations/20260522000020_compliance_schema.sql
@supabase/migrations/20260522000021_compliance_rls.sql
@supabase/migrations/20260522000022_compliance_triggers.sql
@types/auth.ts
@types/compliance.ts
@lib/schemas/compliance.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define audit module contracts first (types + schemas + utility skeleton)</name>
  <files>types/audit.ts, lib/schemas/audit-findings.ts, lib/audit/audit-utils.ts</files>
  <behavior>
    - Test 1: valid severity/status/entity values parse successfully.
    - Test 2: invalid enum values are rejected by Zod schemas.
    - Test 3: utility helpers return deterministic labels and status transition guards.
  </behavior>
  <action>Create `types/audit.ts` with strict union types for severity/status/entity links and label maps. Add `lib/schemas/audit-findings.ts` with schemas for finding creation, status updates, and evidence metadata. Add `lib/audit/audit-utils.ts` for pure helpers (status transition validation, overdue checks, reference formatting). Keep these modules pure and framework-agnostic.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Audit domain contracts compile and can be imported by later service/UI plans without additional type assumptions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add audit schema migration with finding/evidence tables and indexes</name>
  <files>supabase/migrations/20260522000023_audit_schema.sql</files>
  <action>Create enums for severity/status/linked entity and tables `audit_findings` and `audit_finding_evidence`. Include institution-scoped foreign keys, immutable metadata columns, and indexes for status/severity/due_date/owner queries. Ensure finding reference can be unique within an institution to satisfy immutable traceability requirements.</action>
  <verify>
    <automated>npx supabase migration list</automated>
  </verify>
  <done>Migration defines all columns required for AUDIT-01 and AUDIT-02 and is discoverable by Supabase CLI.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add RLS and trigger migrations for audit tables</name>
  <files>supabase/migrations/20260522000024_audit_rls.sql, supabase/migrations/20260522000025_audit_triggers.sql</files>
  <action>Enable and force RLS on new audit tables, enforcing institution filters through `public.institution_id()` and role checks through `public.active_role()` for write paths. Attach audit triggers using existing `audit.attach_audit_trigger()` pattern so every create/update event is captured immutably.</action>
  <verify>
    <automated>npx supabase db push</automated>
  </verify>
  <done>Audit tables are protected by RLS and wired to immutable audit trail triggers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Server Action | Untrusted finding and status payloads enter privileged mutation paths |
| App service role -> Postgres | Incorrect policy design could bypass institution isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | `audit_findings` insert/update | mitigate | Validate payload with Zod, enforce enum constraints, and role gate writes via RLS + action checks |
| T-06-02 | I | RLS policies for audit tables | mitigate | `force row level security` + `institution_id = (select public.institution_id())` in all policies |
| T-06-03 | R | audit trace for findings | mitigate | Attach `audit.attach_audit_trigger()` to both new tables |
| T-06-04 | E | write privilege escalation | mitigate | Restrict write policies to `admin` and `audit-officer` (plus owner transition path where explicitly allowed) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Supabase migrations are listed and apply cleanly in linked environments.
</verification>

<success_criteria>
- Audit schema contracts and security baseline are complete and usable by downstream plans.
- AUDIT-01 and AUDIT-02 backend foundations are fully represented in migrations and type contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/06-internal-audit-findings-and-remediation/06-01-SUMMARY.md`
</output>
