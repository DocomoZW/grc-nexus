---
phase: 08-executive-dashboard-and-reporting-unified-governance-view
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - app/api/reports/governance/route.ts
  - lib/reporting/pdf.ts
  - app/(protected)/dashboard/ExportGovernanceReportButton.tsx
autonomous: true
requirements:
  - RPT-03
must_haves:
  truths:
    - "Users can trigger governance summary report generation from dashboard."
    - "Report output is returned as downloadable PDF response."
    - "PDF payload includes KPI summary, risk heatmap summary table, compliance posture, and overdue actions table."
  artifacts:
    - path: "app/api/reports/governance/route.ts"
      provides: "Role-gated report export endpoint"
      exports: ["GET"]
    - path: "lib/reporting/pdf.ts"
      provides: "PDF builder utility"
      exports: ["buildGovernanceSummaryPdf"]
    - path: "app/(protected)/dashboard/ExportGovernanceReportButton.tsx"
      provides: "Dashboard export action UI"
      exports: ["ExportGovernanceReportButton"]
  key_links:
    - from: "app/api/reports/governance/route.ts"
      to: "lib/reporting/queries.ts"
      via: "fetches consolidated dashboard data before PDF build"
      pattern: "getExecutiveDashboardData"
---

<objective>
Implement phase 8 governance summary PDF export workflow.

Purpose: Deliver RPT-03 from the executive dashboard.
Output: Report export endpoint, PDF utility, and dashboard export button.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-executive-dashboard-and-reporting-unified-governance-view/08-RESEARCH.md
@lib/reporting/queries.ts
@app/api/audit/export/route.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build governance PDF utility and endpoint</name>
  <files>lib/reporting/pdf.ts, app/api/reports/governance/route.ts</files>
  <action>Create a role-gated GET endpoint that validates filters, queries consolidated data, generates PDF bytes, and responds with `Content-Disposition: attachment` plus no-store cache headers.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Endpoint downloads a PDF governance summary.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add dashboard export control</name>
  <files>app/(protected)/dashboard/ExportGovernanceReportButton.tsx, app/(protected)/dashboard/page.tsx</files>
  <action>Add export button wired to the report endpoint and current URL filters. Show pending/disabled states and graceful error handling for non-eligible roles.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Executive users can download filtered governance summary PDF from dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Export endpoint -> report bytes | Unauthorized access can leak governance summaries |
| Filter params -> report query scope | Tampered params can over-broaden report content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | I | report endpoint auth | mitigate | Enforce admin/ceo/audit-officer role checks before querying |
| T-08-06 | T | report filters | mitigate | Validate and normalize filter params using shared schema |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `/api/reports/governance` returns a downloadable PDF for authorized users.
</verification>

<success_criteria>
- RPT-03 is functional via dashboard-driven PDF export.
</success_criteria>

<output>
After completion, create `.planning/phases/08-executive-dashboard-and-reporting-unified-governance-view/08-03-SUMMARY.md`
</output>
