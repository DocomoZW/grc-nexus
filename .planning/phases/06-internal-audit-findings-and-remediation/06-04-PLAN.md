---
phase: 06-internal-audit-findings-and-remediation
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - app/(protected)/audit/findings/[id]/page.tsx
  - app/(protected)/audit/findings/[id]/AuditFindingDetail.tsx
  - app/(protected)/audit/findings/[id]/status/StatusUpdateForm.tsx
  - app/(protected)/audit/findings/[id]/evidence/upload/page.tsx
  - app/(protected)/audit/findings/[id]/evidence/upload/AuditEvidenceUploadForm.tsx
  - components/audit/AuditEvidenceRow.tsx
autonomous: true
requirements:
  - AUDIT-03
must_haves:
  truths:
    - "Remediation owners can update finding status from Open to In Progress to Closed"
    - "Closure evidence can be uploaded and displayed in the finding timeline"
    - "Finding detail preserves immutable audit fields and change history context"
  artifacts:
    - path: "app/(protected)/audit/findings/[id]/page.tsx"
      provides: "Audit finding detail route"
      contains: "status and evidence sections"
    - path: "app/(protected)/audit/findings/[id]/status/StatusUpdateForm.tsx"
      provides: "Controlled status transition form"
      exports: ["StatusUpdateForm"]
    - path: "app/(protected)/audit/findings/[id]/evidence/upload/AuditEvidenceUploadForm.tsx"
      provides: "Evidence upload form with checksum workflow"
      exports: ["AuditEvidenceUploadForm"]
  key_links:
    - from: "app/(protected)/audit/findings/[id]/status/StatusUpdateForm.tsx"
      to: "lib/audit/actions.ts"
      via: "Status mutation"
      pattern: "updateAuditFindingStatus"
    - from: "app/(protected)/audit/findings/[id]/evidence/upload/AuditEvidenceUploadForm.tsx"
      to: "lib/audit/actions.ts"
      via: "Evidence upload action"
      pattern: "uploadAuditFindingEvidence"
---

<objective>
Deliver the remediation execution workflow for each audit finding: detail review, status progression, and closure evidence uploads.

Purpose: Satisfy the operational core of AUDIT-03 with owner-driven remediation tracking.
Output: Finding detail and remediation UI flows linked to audit server actions.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@app/(protected)/compliance/obligations/[id]/page.tsx
@app/(protected)/compliance/obligations/[id]/attest/AttestationForm.tsx
@app/(protected)/compliance/obligations/[id]/evidence/upload/EvidenceUploadForm.tsx
@components/compliance/EvidenceFileRow.tsx
@lib/audit/actions.ts
@lib/audit/queries.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build finding detail route with immutable metadata and remediation context</name>
  <files>app/(protected)/audit/findings/[id]/page.tsx, app/(protected)/audit/findings/[id]/AuditFindingDetail.tsx</files>
  <action>Create detail page that loads finding, owner profile, linked entity data, and evidence list. Display immutable reference/review-date metadata prominently, plus current status, due date, and overdue signal. Include clear handoff to status update and evidence upload subflows.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Each finding has a complete reviewable detail surface with remediation context and immutable identity fields.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement status transition form for remediation owners</name>
  <files>app/(protected)/audit/findings/[id]/status/StatusUpdateForm.tsx</files>
  <action>Implement status transition UI using audit status schema and server action. Restrict options to allowed transitions and provide clear success/error feedback. Revalidate detail/list/dashboard routes after status mutation to keep counts and overdue indicators current.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Remediation owners can progress findings through valid statuses while preserving action auditability.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implement closure evidence upload workflow and evidence row component</name>
  <files>app/(protected)/audit/findings/[id]/evidence/upload/page.tsx, app/(protected)/audit/findings/[id]/evidence/upload/AuditEvidenceUploadForm.tsx, components/audit/AuditEvidenceRow.tsx</files>
  <action>Create upload page/form mirroring compliance checksum and accepted-file workflow. Submit as `FormData` to audit upload action and render evidence metadata rows (filename, uploader, timestamp, checksum snippet). Keep overwrite prevention and checksum verification entirely server-authoritative.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Finding closure evidence can be uploaded and reviewed in context of each finding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remediation owner session -> status mutation | Role confusion could allow non-owners to close findings |
| Browser file upload -> storage path | File tampering and overwrite attempts can occur |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | E | status update action | mitigate | Enforce owner/audit role checks and transition guard logic in server action |
| T-06-13 | T | evidence upload payload | mitigate | Server-side checksum recomputation and immutable path with no upsert |
| T-06-14 | R | finding closure history | mitigate | Keep closure operations as append/update events captured by audit triggers |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Status update and evidence upload routes render for authorized users and reject unauthorized access.
</verification>

<success_criteria>
- Remediation owners can update status and attach closure evidence, fulfilling AUDIT-03 end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/06-internal-audit-findings-and-remediation/06-04-SUMMARY.md`
</output>
